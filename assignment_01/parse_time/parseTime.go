package parsetime

import "time"

// Instruction
// create a function to properly parse a time in the following format. (hours, minutes, seconds month )
func ParseTime(timeStr string, req string) (time.Time, error) {
	// TODO: add your code here
	return time.Time{}, nil
}
